## go dev env

## how to use

```bash
# clone this repo
git clone git@github.com:vinoMamba/my-dev-env.git -b go-env
# open use vscode
# you need to install vscode extention: Dev Container as first
cd my-dev-env
code .
# reopen in container (ctrl + shift + p -> reopen in container)
```

